<section class="card">
  <header>{{recipe_name}}</header>
  <div>{{description}}</div>
  <div>{{ingredients}}</div>
  <div>{{steps}}</div>
  <div>{{img_name}}</div>
  {{!-- <section class="card cb-card">
  
  <h2>{{recipe_name}}</h2>
  <div class="regular-font">{{description}}</div>
  <div class="regular-font">{{ingredients}}</div>
  <div class="regular-font">{{img_name}}</div> --}}
</section>